{{- $reqs := .Reqs -}}
{{- $domainUseCases := domain_use_cases $reqs .Domain.Key -}}
{{- $domainClasses := domain_classes $reqs .Domain.Key -}}

[⇦ {{ .Model.Name }}](model.md)

# {{ if .Domain.Realized }}«realized» {{ end }}{{ .Domain.Name }}

{{ .Domain.Details }}{{ unfinished_notes_block .Domain.UnfinishedNotes }}
{{ if gt (len .Domain.Subdomains) 1 }}
## Subdomains

The subdomains of this domain.

```mermaid
{{ .SubdomainsDiagram }}
```

{{ range .Subdomains -}}
- **[{{ .Name }}]({{ filename "subdomain" .Key "" ".md" }}){{ unfinished_notes_marker .UnfinishedNotes }}.** {{ first_md_sentence .Details }}
{{ end }}

{{ else }}
## Classes

The classes of this domain.

{{ if ne .ClassesDiagram "" }}
```mermaid
{{ .ClassesDiagram }}
```
{{ end }}
{{ range $domainClasses -}}
- **[{{ if ne .ActorKey nil }}«actor» {{ end }}{{ .Name }}]({{ filename "class" .Key "" ".md" }}){{ unfinished_notes_marker .UnfinishedNotes }}.** {{ first_md_sentence .Details }}
{{ end }}

## Use Cases

{{ if ne .UseCasesDiagram "" }}
```mermaid
{{ .UseCasesDiagram }}
```
{{ end }}
{{ range $domainUseCases -}}
- **[{{ if ne .Level "sea" }}«{{ .Level }}» {{ end }}{{ if .ReadOnly }}«read-only» {{ end }}{{ .Name }}]({{ filename "use_case" .Key "" ".md" }}){{ unfinished_notes_marker .UnfinishedNotes }}.** {{ first_md_sentence .Details }}
{{ end }}
{{ end }}

