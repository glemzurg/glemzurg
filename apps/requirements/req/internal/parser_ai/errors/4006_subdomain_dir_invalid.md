# Subdomain Directory Invalid (E4006)

A directory in the `subdomains/` folder does not match the expected naming convention.

## How to Fix

Subdomain directories must be lowercase snake_case: `^[a-z][a-z0-9]*(_[a-z0-9]+)*$`
