# Class Generalization Superclass Is Subclass (E10013)

The `superclass_key` also appears in `subclass_keys`. A class cannot be both the superclass and a subclass in the same generalization.

## How to Fix

Remove the superclass key from the `subclass_keys` array.
