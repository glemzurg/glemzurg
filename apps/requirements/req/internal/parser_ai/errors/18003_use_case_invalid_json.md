# Use Case Invalid JSON (E18003)

The use case file contains invalid JSON syntax.

## How to Fix

Validate the JSON syntax. Common issues: trailing commas, unquoted keys, single quotes instead of double quotes.
