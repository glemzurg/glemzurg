# Subdomain Association Problem Key Empty (E17102)

`problem_subdomain_key` is present but empty or whitespace only.

## How to Fix

Set `problem_subdomain_key` to a non-empty subdomain key that exists under the same domain.