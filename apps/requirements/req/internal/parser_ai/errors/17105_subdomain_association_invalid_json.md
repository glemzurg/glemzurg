# Subdomain Association Invalid JSON (E17105)

The subdomain association file could not be parsed as JSON.

## How to Fix

Ensure the file is valid JSON with `problem_subdomain_key` and `solution_subdomain_key` fields.