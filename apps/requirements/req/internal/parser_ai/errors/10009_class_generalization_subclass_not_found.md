# Class Generalization Subclass Not Found (E10009)

A key in the generalization's `subclass_keys` does not match any class in the subdomain.

## How to Fix

Ensure every key in `subclass_keys` matches a class directory name in the same subdomain.
