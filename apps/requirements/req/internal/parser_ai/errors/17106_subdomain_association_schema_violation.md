# Subdomain Association Schema Violation (E17106)

The subdomain association JSON does not match `subdomain_association.schema.json`.

## How to Fix

Run `req_check --schema subdomain_association` and align the file with the schema.