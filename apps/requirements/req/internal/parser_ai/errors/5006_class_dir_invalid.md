# Class Directory Invalid (E5006)

A directory in the `classes/` folder does not match the expected naming convention.

## How to Fix

Class directories must be lowercase snake_case: `^[a-z][a-z0-9]*(_[a-z0-9]+)*$`
