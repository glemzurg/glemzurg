# Association From-Class Not Found (E6009)

The association's `from_class_key` does not match any class in the current scope.

## How to Fix

Ensure `from_class_key` matches a class directory name in the same scope (model, domain, or subdomain).
