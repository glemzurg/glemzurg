# Subdomain Association Solution Key Empty (E17104)

`solution_subdomain_key` is present but empty or whitespace only.

## How to Fix

Set `solution_subdomain_key` to a non-empty subdomain key that exists under the same domain.