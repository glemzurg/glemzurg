# Domain Directory Invalid (E3006)

A directory in the `domains/` folder does not match the expected naming convention.

## How to Fix

Domain directories must be lowercase snake_case: `^[a-z][a-z0-9]*(_[a-z0-9]+)*$`
