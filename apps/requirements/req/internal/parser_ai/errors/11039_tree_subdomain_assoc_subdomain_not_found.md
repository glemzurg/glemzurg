# Subdomain Association References Missing Subdomain (E11039)

A subdomain association references a subdomain key that does not exist in the domain.

## How to Fix

Create the missing subdomain under `domains/{domain}/subdomains/` or correct the key in the association file.