# Use Case Schema Violation (E18004)

The use case JSON does not conform to the expected schema.

## How to Fix

Check the schema with `req_check --schema use_case` and ensure all required fields are present with valid values.
