# Domain Duplicate Key (E3005)

Two domain directories resolve to the same key. Domain keys are derived from directory names.

## How to Fix

Each domain directory must have a unique name. Rename or remove the duplicate.
