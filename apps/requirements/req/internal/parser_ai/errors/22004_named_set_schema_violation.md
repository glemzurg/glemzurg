# Named Set Schema Violation (E22004)

The named set JSON does not conform to the expected schema.

## How to Fix

Check the schema with `req_check --schema named_set` and ensure all fields are valid.
