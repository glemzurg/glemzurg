# Use Case Name Required (E18001)

The use case JSON file is missing the required `name` field.

## How to Fix

Add a `name` field:

```json
{
    "name": "Place Order",
    "level": "user_goal"
}
```
