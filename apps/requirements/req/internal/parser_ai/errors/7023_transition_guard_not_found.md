# Transition Guard Not Found (E7023)

A transition references a `guard_key` that does not exist in the guards map.

## How to Fix

Ensure `guard_key` matches a key in the `guards` map, or remove it if no guard is needed.
