# Named Set Name No Leading Underscore (E22005)

The named set key starts with an underscore, which is reserved.

## How to Fix

Named set keys must not start with an underscore. Use lowercase snake_case: `order_statuses`, not `_order_statuses`.
