# Subdomain Associations Require Multiple Subdomains (E11040)

The domain has subdomain association files but fewer than two subdomains.

## How to Fix

Add another subdomain to the domain or remove subdomain association files. A subdomain cannot depend on itself, and single-subdomain domains have no valid target.