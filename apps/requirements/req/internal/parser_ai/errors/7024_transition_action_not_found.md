# Transition Action Not Found (E7024)

A transition references an `action_key` that does not exist in the class's actions.

## How to Fix

Ensure `action_key` matches an action file in the class's `actions/` directory.
